# include <stdio.h>
# include <math.h>
#define pf printf
#define sf scanf
int main()
{
	long long int n,a;
	sf("%lld",&n);
	while(n--)
	{
		sf("%lld",&a);
		if(a==1)
			pf("1\n");
		else
			pf("%lld\n", ( (a*a)/2 - 1) );
 
	}
 
}
