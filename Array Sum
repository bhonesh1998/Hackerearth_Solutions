#include<stdio.h>
#include <string.h>
#define pf printf
#define sf scanf
 
int main()
{ 
 
 
 
	long long int n,s=0,i;
	sf("%lld",&n);
	long long int a[n];
	for(i=0;i<n;i++)
		sf("%lld",&a[i]);
	for(i=0;i<n;i++)
		s+=a[i];
	pf("%lld",s);
		
 
 
 
 
 
 
 
 
 
 
} 
