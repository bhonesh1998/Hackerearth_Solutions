#include<stdio.h>
#include <string.h>
#define pf printf
#define sf scanf
int main()
{ 
 
	char c[1000];
	sf("%s",c);
	int f=0;
	for(int i=0;i<strlen(c);i++)
	{
		if(c[i]!=c[strlen(c)-i-1])
			f=1;
	}
if(f==0)
	pf("YES");
else
pf("NO");
 
 
}
